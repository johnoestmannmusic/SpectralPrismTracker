// GENERATED BY constraints-generate-tests — DO NOT EDIT THIS FILE.
//
// Source: CONSTRAINTS.md (sha256 98922ca249d6)
// Rule:   [HC005] (blocking) The web-hosted version must be a static client-side webpage. No shared terminal.
// Why:    multiple people need to be able to access the site and experience it individually, and we need to prevent security issues of shared access. Fate is per-visitor: no server process owns a Session, no signal crosses visitors.
//
// Regenerate with the constraints_generate_tests tool.
// Hand-written assertions belong in ../checks/ — never overwritten.

import { test } from "node:test";
import { assertCommandExit, assertNoForbiddenContent, assertNoMatchingFiles, assertRequiredContent } from "./_harness.mjs";

test("[HC005] must not exist: src/web/server.tsx", async () => {
	await assertNoMatchingFiles(["src/web/server.tsx"]);
});

test("[HC005] does not contain node:http in src/web/**", async () => {
	await assertNoForbiddenContent("node:http", ["src/web/**"], undefined);
});

test("[HC005] does not contain createServer in src/web/**", async () => {
	await assertNoForbiddenContent("createServer", ["src/web/**"], undefined);
});

test("[HC005] does not contain setHost(nodeHost) in src/web/**", async () => {
	await assertNoForbiddenContent("setHost(nodeHost)", ["src/web/**"], undefined);
});

test("[HC005] contains browserHost in src/web/**", async () => {
	await assertRequiredContent("browserHost", ["src/web/**"], false);
});

test("[HC005] runs `npm run test:web` and expects exit 0", async () => {
	await assertCommandExit("npm run test:web", 0);
});
