/**
 * Node runtime file-layer. The implementation now lives in the Node host
 * (`src/host/node/files.ts`); this module stays as the stable import site so
 * existing Node code and tests keep working unchanged.
 */
export * from "@/host/node/files";
